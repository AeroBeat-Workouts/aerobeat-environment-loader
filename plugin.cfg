[plugin]

name="AeroBeat Environment Loader"
description="Environment loader/orchestrator package for AeroBeat. Consumes aerobeat-environment-core contracts, fulfills image through the shared AeroImageLoader facade, fulfills GLB through the shared AeroGLTFLoader facade, fulfills video through AeroVideoPlayerManager, and keeps workout YAML bridging."
author="AeroBeat Workouts"
version="0.2.0"
